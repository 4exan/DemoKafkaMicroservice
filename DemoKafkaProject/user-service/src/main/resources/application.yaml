server:
  port: 8081
eureka:
  instance:
    hostname: localhost
spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/kafkademo
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update